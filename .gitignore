# Никогда не попадает в репозиторий: пароль от роутера (шифрован Windows DPAPI,
# но это всё равно файл с учётными данными).
cred.xml

# Реальный список устройств с MAC-адресами домашней сети.
# В репозитории лежит только devices.example.json.
devices.json

# Ярлыки, создаваемые make-shortcuts.ps1
*.lnk

# Личные заметки, не относящиеся к проекту
/notes/
*.local.txt

# Windows
Thumbs.db
desktop.ini
